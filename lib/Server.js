
class AMFServer {

    /*
        This is the AMF0/AMF3 Server class. Use this class to construct a gateway for clients to connect to!
    */

    constructor(host, port) {
        //this.amfRequest = new AMFMessage();
        /*
            TODO: 
                -Run an express server that listens for requests
                -Read each request
                -Maybe add a callback param so that after each request is read and received, the callback function is called with the deserialized request as a parameter
                -note to set header content type as application amf
                
        */
    }

}

module.exports = AMFServer;